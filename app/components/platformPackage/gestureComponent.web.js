import {
  ScrollView,
  FlatList,
  TextInput,
  TouchableWithoutFeedback,
} from 'react-native';

export const ScrollView_P = ScrollView;
export const FlatList_P = FlatList;
export const TextInput_P = TextInput;
export const TouchableWithoutFeedback_P = TouchableWithoutFeedback;
