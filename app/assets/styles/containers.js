import {
  getH,
  getWFloat,
  WINDOW_HEIGHT,
  WINDOW_WIDTH,
} from '@constants/appUnits';
import {getW} from '@constants/appUnits';
import COLORS from './colors';
import SHADOW from './shadow';

export const CONTAINER = {

};
